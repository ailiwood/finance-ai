# COMBINED_TEST_CHECKLIST.md — 合并验收清单(数据修复 + CPU/GPU 部署)

> 放到项目 `docs/`。本清单合并验证两批改动:① 之前已写完但你还没测的(round9 数据传输修复 + round10 CPU验证)② 本轮新增(硬件检测 + GPU可选升级)。一次全量测试覆盖。

---

## 背景
CC 已汇报完成"数据传输修复(tuple bug)、禁编造、CPU适配验证",但开发者尚未测试。本轮加入"硬件检测+GPU可选"后,**一起做一次全量测试**,避免反复打包。

---

## A. 数据链路验证(验证之前未测的修复)
- [ ] get_kline 返回 DataFrame(不再是元组),无 `'tuple' object has no attribute 'split'` 错误
- [ ] 分析 600519,技术面报告含**真实** MA5/MACD/RSI 和价格(与同花顺前复权一致,~1215-1250)
- [ ] 数据来源显示 BaoStock、复权方式显示前复权(读 df.attrs)
- [ ] 数据体检页最近10日 OHLCV 与同花顺逐行一致
- [ ] 情绪面:接通了免费新闻源并出真实情绪;**或**明确报错"情绪数据暂不可用"——**绝不是编造的分数**(如之前的"6.5/10")
- [ ] 全模块(技术/情绪/基本面/风控):数据缺失时报错,不编造

## B. CPU 部署验证(无显卡也能用)
- [ ] 打包/环境用的是 CPU 版 torch(非 CUDA 版)
- [ ] 全项目无硬编码 cuda,推理用 pick_device()
- [ ] **强制 CPU 模式(CUDA_VISIBLE_DEVICES=-1)下**:Kronos 出预测、FinBERT 出情绪分数,无 CUDA 报错(贴真实输出)
- [ ] CPU 模式下 Kronos 预测值合理(茅台~1200-1300),记录耗时

## C. 硬件检测 + GPU 可选(本轮新增)
- [ ] 应用启动检测硬件,UI 正确显示算力模式:
  - [ ] 无N卡 → "CPU模式"
  - [ ] 有N卡未升级 → "检测到{显卡},可升级GPU版"
  - [ ] 有N卡已升级 → "GPU加速已启用"
- [ ] "升级GPU版"按钮存在,点击有明确提示(体积/耗时/驱动要求)
- [ ] (你的5070Ti机器)升级后 torch.cuda.is_available()=True,Kronos 走 GPU 更快
- [ ] 不升级时,CPU 版全功能可用(Kronos只是慢些)
- [ ] 升级失败有清晰提示且不破坏 CPU 版

## D. 端到端(两插件全开)
- [ ] streamlit run,开启 K线预测 + 情绪分析,分析 600519 全流程跑通
- [ ] 四个模块都有真实输出或明确报错(无编造)
- [ ] 红色免责声明、版权页脚在每页显示

---

## 测试方式
1. **先在开发机源码层面(streamlit run)跑通 A/B/C/D**,CC 把结果填进下面的表。
2. 源码层过了再打包(onedir, CPU版torch),开发者本地安装复测。
3. 有条件的话,在一台无N卡电脑上验证 B(CPU部署)——最硬的证据。

## CC 填写的结果表
```
| 项 | 状态 | 真实输出/说明 |
|----|------|--------------|
| A 数据链路 | ✅/❌ | MA5值=?, 来源=?, 情绪=真实/报错 |
| B CPU部署 | ✅/❌ | CUDA_VISIBLE_DEVICES=-1下 Kronos输出=?, 耗时=? |
| C 硬件检测 | ✅/❌ | 检测到=?, UI显示=? |
| D 端到端 | ✅/❌ | 四模块状态 |
```
